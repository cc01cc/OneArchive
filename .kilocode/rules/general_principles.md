# 通用开发原则

- 所有代码必须基于项目当前实际使用的依赖库版本，禁止使用已废弃（deprecated）或未来版本的 API。
- 在回答或生成代码前，必须确认上下文中的依赖版本（如 `Cargo.toml`、`package.json`）。
- 优先复用项目中已有的函数、结构体、组件或工具方法，避免重复造轮子。
- 代码必须逻辑清晰、简洁、高可读性；避免过度工程或炫技式写法。
- 若发现现有代码存在逻辑问题、架构缺陷或潜在风险，应主动指出，而非强行生成“看似正确”的代码。
